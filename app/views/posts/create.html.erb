<!-- Create Post -->

<div class="form-page">
    <!-- Title -->
    <%= render "shared/main_header" do %>
        <%= @pageTitle %>
    <% end %>

    <!-- Post creation form -->
    <form>
        <!-- Url -->
        <!-- TODO!!!  render pot escriure's render("shared/form/text_field") i possiblememt accepti més paràmetres -->
        <!-- TODO!!!  scafold -->
        <%= render "shared/form/text_field", label: @urlLabel %>

        <!-- Title -->
        <%= render "shared/form/text_field", label: @titleLabel %>

        <!-- Body -->
        <%= render "shared/form/text_area", label: @bodyLabel %>
    </form>

    <!-- Create button -->
    <%= render "/shared/button" do %>
        <%= @submitButton %>
    <% end %>

    <%= render "form", post: @post %>

</div>